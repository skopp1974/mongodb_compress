# Load test

The canonical documentation for setup, config keys, `results.json`, and cleanup is:

- `../README.md`

Files in this folder:

- `config.example.yaml` / `config.yaml`
- `load_test/` Python package (runner)
- `clear_db.py` helper
